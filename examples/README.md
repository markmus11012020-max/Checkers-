# Examples

Run the game from the repository root:

```powershell
python src\main.py
```

Example moves:

- `b6-a5` — move a black piece forward left
- `b6xc4` — capture a white piece by jumping

To stop the game, type `exit` or `quit`.
